# backend
Proyecto backend reactivo desarrollado en Java 21 y Spring Boot (WebFlux). Implementa una Arquitectura Hexagonal para el registro de usuarios (asíncrona en H2 vía R2DBC) y establece una comunicación segura mediante mTLS hacia AWS API Gateway y AWS Lambda. 
